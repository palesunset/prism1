{
  "name": "prism-inventory",
  "private": true,
  "version": "1.0.0",
  "description": "PRISM Network Equipment Inventory module",
  "scripts": {
    "dev": "npx concurrently -n inv-api,inv-web -c magenta,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "test": "npm run test --prefix backend && npm run test --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
